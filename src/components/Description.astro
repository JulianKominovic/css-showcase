---
const { title, description } = Astro.props;
---

<header>
  {
    title && (
      <p class="mt-4 text-xl font-bold tracking-tight text-gray-100 sm:text-4xl">
        {title}
      </p>
    )
  }
  {description && <p class="max-w-2xl text-xl text-gray-500">{description}</p>}
</header>
